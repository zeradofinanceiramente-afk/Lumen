rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ MODO DE DESENVOLVIMENTO: ACESSO TOTAL LIBERADO
    // Todas as validações foram removidas temporariamente.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}